<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Cập nhật thông tin cá nhân</title>
</head>
<body>
    <h1>Cập nhật thông tin cá nhân</h1>
    <form action="/capnhat" method="POST">
        <div>
            <label for="hoten">Họ tên:</label>
            <input type="text" id="hoten" name="hoten" value="<%= user.hoten %>" required>
        </div>
        <div>
            <label for="namsinh">Năm sinh:</label>
            <input type="number" id="namsinh" name="namsinh" value="<%= user.namsinh %>" required>
        </div>
        <div>
            <label for="gioitinh">Giới tính:</label>
            <select id="gioitinh" name="gioitinh">
                <option value="male" <%= user.gioitinh === 'male' ? 'selected' : '' %>>Nam</option>
                <option value="female" <%= user.gioitinh === 'female' ? 'selected' : '' %>>Nữ</option>
            </select>
        </div>
        <div>
            <label for="email">Email (không thể thay đổi):</label>
            <input type="email" id="email" value="<%= user.email %>" disabled>
        </div>
        <button type="submit">Cập nhật</button>
    </form>
</body>
</html>
